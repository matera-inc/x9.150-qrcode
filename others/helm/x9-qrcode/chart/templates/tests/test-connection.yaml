apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "x9-qrcode.fullname" . }}-test-connection"
  labels:
    {{- include "x9-qrcode.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "x9-qrcode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
